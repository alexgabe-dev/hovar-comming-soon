/** @type {import('postcss-load-config').Config} */
const config = {
  // PostCSS plugins - Tailwind CSS processing
  plugins: {
    tailwindcss: {}, // Tailwind CSS plugin
  },
};

export default config;
